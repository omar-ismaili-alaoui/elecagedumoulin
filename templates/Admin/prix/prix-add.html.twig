{% extends 'base-admin.html.twig' %}

{% block body %}
    <h1 class="page-h<eader">Ajouter un prix</h1>
    {{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.race) }}
    </div>
    <div class="form-group">
        {{ form_row(form.prix) }}
    </div>
    <button type="submit" class="btn btn-success">{% if state == 'new' %}Ajouter{% else %}Enregistrer{% endif %}</button>
    <a href="{{ path('admin_prix') }}" class="btn btn-primary">Retour</a>
    {{ form_end(form) }}
{% endblock %}
